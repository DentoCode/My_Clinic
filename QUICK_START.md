# دليل الاستخدام السريع - تطبيق عيادة الأسنان

## خطوات التشغيل الأولى 🚀

### 1. التأكد من تثبيت Flutter
```bash
flutter doctor
```
يجب أن ترى علامة ✓ أمام Flutter و Android toolchain

### 2. الانتقال إلى مجلد المشروع
```bash
cd dental_clinic_app
```

### 3. تثبيت المكتبات
```bash
flutter pub get
```

### 4. تشغيل التطبيق
```bash
# على محاكي/جهاز متصل
flutter run

# أو في وضع التطوير
flutter run -d chrome  # للويب
flutter run -d windows  # لويندوز
```

## بيانات الدخول الافتراضية 🔑

```
اسم المستخدم: admin
كلمة المرور: admin123
```

## الوظائف الأساسية 📋

### إدارة المرضى
1. من الشاشة الرئيسية، اضغط على "إدارة المرضى"
2. للإضافة: اضغط زر "+" في الأسفل
3. للبحث: استخدم شريط البحث في الأعلى
4. للتعديل: اضغط على المريض، ثم أيقونة التعديل
5. للحذف: اضغط على النقاط الثلاث، ثم "حذف"

### حجز المواعيد
1. اذهب إلى "المواعيد"
2. اختر التاريخ من التقويم
3. اضغط "حجز موعد"
4. اختر المريض والوقت ونوع الموعد
5. احفظ

### تسجيل العلاجات
1. اذهب إلى "العلاجات"
2. اضغط "إضافة علاج"
3. اختر المريض
4. حدد نوع العلاج ورقم السن
5. أدخل التكلفة والمبلغ المدفوع
6. احفظ

### إضافة دفعة لعلاج
1. في شاشة العلاجات
2. اضغط على العلاج لتوسيعه
3. اضغط "إضافة دفعة"
4. أدخل المبلغ
5. احفظ

## حل المشاكل الشائعة 🔧

### مشكلة: التطبيق لا يعمل
**الحل:**
```bash
flutter clean
flutter pub get
flutter run
```

### مشكلة: خطأ في قاعدة البيانات
**الحل:**
- احذف التطبيق من الجهاز
- أعد تشغيله من جديد
- سيتم إنشاء قاعدة بيانات جديدة

### مشكلة: اللغة العربية لا تظهر بشكل صحيح
**الحل:**
- تأكد من إضافة مكتبة intl
- أعد تشغيل التطبيق

### مشكلة: التقويم لا يظهر بالعربية
**الحل:**
- تأكد من تثبيت مكتبة table_calendar
- تحقق من إعدادات اللغة في الكود

## نصائح للاستخدام الأمثل 💡

### 1. النسخ الاحتياطي
- قاعدة البيانات موجودة في مجلد التطبيق
- يمكنك نسخ ملف `dental_clinic.db` للاحتفاظ بنسخة احتياطية

### 2. إدخال البيانات
- استخدم معلومات دقيقة للمرضى
- احرص على تحديث رقم الهاتف دائماً
- سجل الحساسية والتاريخ الطبي المهم

### 3. المواعيد
- احجز المواعيد مسبقاً
- حدث حالة الموعد بعد الانتهاء
- استخدم الملاحظات لتسجيل معلومات إضافية

### 4. العلاجات
- سجل كل علاج فوراً بعد إتمامه
- حدد رقم السن بدقة
- سجل المدفوعات أولاً بأول

### 5. التقارير
- راجع التقارير دورياً
- تابع المبالغ المستحقة
- استخدم الإحصائيات لتحسين الأداء

## تخصيص التطبيق ⚙️

### تغيير الألوان
افتح ملف `lib/main.dart` وعدل:
```dart
primarySwatch: Colors.blue,  // غير اللون هنا
```

### إضافة أنواع علاج جديدة
افتح `lib/screens/treatments_screen.dart` وأضف في القائمة:
```dart
DropdownMenuItem(value: 'نوع جديد', child: Text('نوع جديد')),
```

### تعديل بيانات الدخول
افتح `lib/database/database_helper.dart` وعدل:
```dart
'username': 'admin',
'password': 'admin123',
```

## البناء للإنتاج 📦

### Android (APK)
```bash
flutter build apk --release
```
الملف سيكون في: `build/app/outputs/flutter-apk/app-release.apk`

### Android (App Bundle)
```bash
flutter build appbundle --release
```

### iOS
```bash
flutter build ios --release
```

### Windows
```bash
flutter build windows --release
```

## الدعم الفني 📞

### في حالة وجود مشاكل:
1. تحقق من ملف README.md
2. راجع رسائل الخطأ في Terminal
3. استخدم `flutter doctor` للتحقق من الإعدادات
4. تأكد من تحديث Flutter للنسخة الأخيرة

### أوامر مفيدة:
```bash
# تحديث Flutter
flutter upgrade

# تنظيف المشروع
flutter clean

# التحقق من المشاكل
flutter analyze

# عرض الأجهزة المتصلة
flutter devices
```

## الخلاصة ✅

التطبيق جاهز للاستخدام مباشرة بعد تشغيل `flutter pub get` و `flutter run`

تذكر:
- بيانات الدخول: admin / admin123
- البيانات مخزنة محلياً على الجهاز
- النسخ الاحتياطي مهم جداً

**بالتوفيق! 🎉**
