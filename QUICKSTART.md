# تعليمات سريعة للبدء 🚀

## خطوات التشغيل السريعة

### 1. في VS Code
```bash
# افتح المشروع
cd dental_clinic_app
code .
```

### 2. تثبيت المكتبات
في Terminal في VS Code:
```bash
flutter pub get
```

### 3. تشغيل التطبيق
اختر واحدة من الطرق التالية:

**الطريقة الأولى (الأسهل):**
- اضغط F5 في VS Code

**الطريقة الثانية:**
```bash
flutter run
```

**الطريقة الثالثة:**
- من قائمة VS Code: Run > Start Debugging

### 4. بيانات تسجيل الدخول
```
اسم المستخدم: admin
كلمة المرور: 1234
```

## إذا واجهت مشاكل ⚠️

### المشكلة: لا يظهر المحاكي
**الحل:**
```bash
# تأكد من تثبيت Flutter
flutter doctor

# قائمة الأجهزة المتاحة
flutter devices

# تشغيل محاكي Android
flutter emulators --launch <emulator_id>
```

### المشكلة: أخطاء في المكتبات
**الحل:**
```bash
# تنظيف المشروع
flutter clean

# إعادة تحميل المكتبات
flutter pub get

# إعادة التشغيل
flutter run
```

### المشكلة: الخطوط العربية لا تظهر بشكل صحيح
**الحل:**
الخطوط الافتراضية للنظام تدعم العربية، لكن إذا أردت خط مخصص:
1. حمل خط Cairo من Google Fonts
2. ضعه في مجلد `fonts/` في المشروع
3. التطبيق معد لاستخدامه

## هيكل المشروع 📂

```
dental_clinic_app/
│
├── lib/
│   ├── main.dart                    ← نقطة البداية
│   │
│   ├── models/                      ← نماذج البيانات
│   │   ├── patient.dart
│   │   ├── appointment.dart
│   │   ├── treatment.dart
│   │   └── payment.dart
│   │
│   ├── database/                    ← قاعدة البيانات
│   │   └── database_helper.dart
│   │
│   └── screens/                     ← واجهات التطبيق
│       ├── login_screen.dart
│       ├── home_screen.dart
│       ├── patients_screen.dart
│       ├── add_edit_patient_screen.dart
│       ├── patient_details_screen.dart
│       ├── appointments_screen.dart
│       ├── payments_screen.dart
│       └── reports_screen.dart
│
├── pubspec.yaml                     ← إعدادات المشروع
└── README.md                        ← الدليل الكامل
```

## الشاشات الرئيسية 📱

### 1. شاشة تسجيل الدخول
- أول شاشة تظهر
- تسجيل الدخول بـ admin/1234

### 2. الشاشة الرئيسية (Dashboard)
- إحصائيات سريعة
- أزرار للانتقال إلى الأقسام

### 3. شاشة المرضى
- قائمة جميع المرضى
- بحث بالاسم أو الهاتف
- إضافة/تعديل/حذف

### 4. شاشة تفاصيل المريض
- معلومات المريض
- مواعيده
- علاجاته والمدفوعات

### 5. شاشة المواعيد
- جدول يومي
- التنقل بين الأيام
- حالة كل موعد

## نصائح مهمة 💡

1. **البيانات محلية:** كل البيانات محفوظة على جهازك فقط
2. **لا يحتاج إنترنت:** التطبيق يعمل بدون اتصال
3. **التطوير:** يمكنك تعديل أي جزء حسب احتياجك
4. **الألوان:** سهل تغيير الألوان من ملف main.dart

## أوامر مفيدة 🔧

```bash
# فحص Flutter
flutter doctor

# تنظيف المشروع
flutter clean

# تحديث المكتبات
flutter pub upgrade

# بناء APK للأندرويد
flutter build apk

# تشغيل على جهاز فعلي
flutter run

# تشغيل مع Hot Reload السريع
flutter run --hot
```

## خطوات التطوير التالية 🔮

1. ✅ أضف مرضى تجريبيين
2. ✅ جرب إضافة مواعيد
3. ✅ استكشف كل الشاشات
4. 🔄 أضف مميزات جديدة حسب احتياجك
5. 🔄 غيّر الألوان والتصميم

---

**ملاحظة مهمة:**
إذا كانت هذه أول مرة تشغل فيها Flutter، قد تأخذ عملية التشغيل الأولى وقتاً (5-10 دقائق)
بسبب تحميل وبناء المكتبات. المرات القادمة ستكون أسرع بكثير!

**للمساعدة:**
- راجع ملف README.md للتفاصيل الكاملة
- استخدم `flutter doctor` للتحقق من الإعداد
- تأكد من تشغيل المحاكي قبل `flutter run`
