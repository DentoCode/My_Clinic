# خطوات إعداد المشروع في VS Code

## 1. فتح المشروع في VS Code

1. افتح VS Code
2. اذهب إلى: File → Open Folder
3. اختر مجلد `dental_clinic_app`

## 2. تثبيت المكتبات

في Terminal داخل VS Code (Ctrl + `):

```bash
flutter pub get
```

انتظر حتى تكتمل عملية تحميل جميع المكتبات.

## 3. إعداد المحاكي

### Android:
```bash
# عرض المحاكيات المتاحة
flutter emulators

# تشغيل محاكي معين
flutter emulators --launch <emulator_id>
```

### أو استخدم Android Studio لتشغيل محاكي

## 4. تشغيل التطبيق

### الطريقة الأولى: من Terminal
```bash
flutter run
```

### الطريقة الثانية: من VS Code
1. اضغط F5
2. أو اضغط على Run → Start Debugging

### الطريقة الثالثة: من الأيقونة
اضغط على أيقونة ▶️ في الزاوية اليمنى العليا

## 5. اختبار التطبيق

1. انتظر حتى يتم تشغيل التطبيق على المحاكي
2. استخدم بيانات الدخول:
   - اسم المستخدم: `admin`
   - كلمة المرور: `admin123`

## 6. Hot Reload

أثناء التطوير:
- اضغط `r` في Terminal لعمل Hot Reload
- اضغط `R` في Terminal لعمل Hot Restart
- أو استخدم Ctrl+S لحفظ الملف وسيتم Hot Reload تلقائياً

## هيكل المشروع

```
dental_clinic_app/
│
├── lib/
│   ├── main.dart                      # نقطة البداية
│   │
│   ├── models/                        # نماذج البيانات
│   │   ├── patient.dart              # نموذج المريض
│   │   ├── appointment.dart          # نموذج الموعد
│   │   ├── treatment.dart            # نموذج العلاج
│   │   └── payment.dart              # نموذج الدفع
│   │
│   ├── database/                      # قاعدة البيانات
│   │   └── database_helper.dart      # إدارة SQLite
│   │
│   └── screens/                       # الشاشات
│       ├── login_screen.dart         # شاشة تسجيل الدخول
│       ├── home_screen.dart          # الشاشة الرئيسية
│       ├── patients_screen.dart      # شاشة المرضى
│       ├── patient_details_screen.dart
│       ├── appointments_screen.dart  # شاشة المواعيد
│       ├── treatments_screen.dart    # شاشة العلاجات
│       ├── payments_screen.dart      # شاشة المدفوعات
│       └── reports_screen.dart       # شاشة التقارير
│
├── pubspec.yaml                       # ملف المكتبات
├── README.md                          # الوثائق
├── QUICK_START.md                     # دليل سريع
└── SETUP_INSTRUCTIONS.md              # هذا الملف
```

## المكتبات المستخدمة

التطبيق يستخدم المكتبات التالية:

- **sqflite**: قاعدة البيانات المحلية
- **path_provider**: الوصول إلى مسارات النظام
- **provider**: إدارة الحالة
- **intl**: دعم اللغة العربية والتواريخ
- **table_calendar**: التقويم التفاعلي
- **shared_preferences**: حفظ الإعدادات
- **uuid**: توليد معرفات فريدة
- **fl_chart**: الرسوم البيانية
- **pdf**: إنشاء ملفات PDF
- **printing**: الطباعة

## ملاحظات مهمة

### 1. اللغة العربية
التطبيق مصمم بالكامل للغة العربية مع دعم RTL (من اليمين لليسار)

### 2. قاعدة البيانات
- يتم إنشاء قاعدة البيانات تلقائياً عند أول تشغيل
- موقع قاعدة البيانات: داخل مجلد التطبيق على الجهاز
- اسم الملف: `dental_clinic.db`

### 3. مستخدم افتراضي
يتم إنشاء مستخدم افتراضي تلقائياً:
- Username: admin
- Password: admin123

### 4. البيانات التجريبية
التطبيق لا يحتوي على بيانات تجريبية. يمكنك البدء بإضافة:
1. مرضى جدد
2. حجز مواعيد
3. تسجيل علاجات

## استكشاف الأخطاء

### خطأ: "Waiting for another flutter command to release the startup lock"
```bash
# حذف ملف القفل
rm flutter.lock

# أو
flutter clean
```

### خطأ: "Unable to locate Android SDK"
- افتح Android Studio
- اذهب إلى SDK Manager
- تأكد من تثبيت Android SDK

### خطأ: "No devices found"
```bash
# للتحقق من الأجهزة المتصلة
flutter devices

# لتشغيل محاكي
flutter emulators --launch <emulator_id>
```

### خطأ في المكتبات
```bash
# تنظيف وإعادة تثبيت
flutter clean
flutter pub get
```

## التطوير والتعديل

### إضافة ميزة جديدة
1. أنشئ ملف dart جديد في المجلد المناسب
2. استورد الملفات المطلوبة
3. أنشئ الـ Widget أو Class
4. استخدمه في الشاشة المطلوبة

### تعديل قاعدة البيانات
افتح `lib/database/database_helper.dart`:
- لإضافة جدول جديد: عدل دالة `_createDB`
- لإضافة عمود: زد رقم الإصدار وأنشئ migration

### تخصيص الألوان والتصميم
افتح `lib/main.dart` وعدل في:
```dart
theme: ThemeData(
  primarySwatch: Colors.blue,  // اللون الأساسي
  // ... باقي الإعدادات
)
```

## البناء للإصدار

### Android APK
```bash
flutter build apk --release
```

### Android App Bundle (للنشر على Google Play)
```bash
flutter build appbundle --release
```

### iOS
```bash
flutter build ios --release
```

### Windows Desktop
```bash
flutter build windows --release
```

## نصائح للتطوير

1. **استخدم Hot Reload**: احفظ الملف (Ctrl+S) لرؤية التغييرات فوراً
2. **راقب Console**: تابع رسائل الخطأ في Terminal
3. **استخدم Debugger**: ضع Breakpoints لتتبع الكود
4. **اختبر على أجهزة حقيقية**: للتأكد من الأداء
5. **راجع الكود**: استخدم `flutter analyze` للتحقق من جودة الكود

## الموارد المفيدة

- [Flutter Documentation](https://flutter.dev/docs)
- [Dart Language Tour](https://dart.dev/guides/language/language-tour)
- [SQLite في Flutter](https://flutter.dev/docs/cookbook/persistence/sqlite)
- [Material Design بالعربية](https://material.io/design)

## الدعم

في حالة وجود مشاكل:
1. راجع ملف README.md
2. راجع ملف QUICK_START.md
3. تحقق من `flutter doctor`
4. ابحث عن الخطأ في Google

---

**مع تمنياتي بالتوفيق في تطوير التطبيق! 🚀**
